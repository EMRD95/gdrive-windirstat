// config.example.js — copy this to config.js and fill in your OAuth client ID.
// config.js is gitignored so your client ID never hits the repo.
//
// Get a client ID at: https://console.cloud.google.com/apis/credentials
//   1. Create project → APIs & Services → Credentials → Create OAuth client ID
//   2. Application type: Web application
//   3. Authorized JavaScript origins: http://localhost:8765 (dev) and your prod URL
//   4. Copy the client ID here.

export const CLIENT_ID = 'YOUR_CLIENT_ID.apps.googleusercontent.com';
